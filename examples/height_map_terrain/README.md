# Height Map Terrain

Basic height map terrain on a grid.
Utilizes the [noise](https://docs.rs/noise/0.9.0/noise/) library to generate simplex noise.
Run with `--render-distance <distance>` to increase/decrease render distance. Large values (>= 20) will cause framerates to decrease dramatically.
Run with `--speed <s>` to increase/decrease movement speed.
